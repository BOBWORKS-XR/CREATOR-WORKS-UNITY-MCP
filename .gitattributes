# Preserve the official brand asset byte-for-byte across platform checkouts.
launcher/src/sidequest-mark-white.svg -text
# Exact-match helper validation must use identical bytes on every platform.
launcher/unity/com.creatorworks.plugins/** -text
